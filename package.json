{
  "name": "nicos-desktop",
  "version": "0.2.2",
  "description": "NicOS Desktop — consola de Nicolás (Director) y Marianela (Operativa), con Claude y OpenAI.",
  "main": "electron/main.js",
  "author": "Nicolás Buso",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "start": "electron .",
    "build-info": "node scripts/generate-build-info.js",
    "dist:mac": "npm run build-info && electron-builder --mac",
    "dist:win": "npm run build-info && electron-builder --win --dir && node scripts/verify-win-package.js dist && electron-builder --win"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-builder": "^24.13.3"
  },
  "build": {
    "appId": "com.nicolasbuso.nicos-desktop",
    "productName": "NicOS Desktop",
    "artifactName": "NicOS-Desktop-${version}-${arch}.${ext}",
    "files": [
      "electron/**/*",
      "renderer/**/*",
      "package.json"
    ],
    "mac": {
      "category": "public.app-category.productivity",
      "target": "dmg",
      "extraResources": [
        {
          "from": "sidecar/dist/nicos-sidecar",
          "to": "sidecar/nicos-sidecar"
        },
        {
          "from": "policies",
          "to": "policies"
        },
        {
          "from": "build/build-info.json",
          "to": "build-info.json"
        }
      ]
    },
    "win": {
      "target": "nsis",
      "extraResources": [
        {
          "from": "build/build-info.json",
          "to": "build-info.json"
        }
      ],
      "publish": {
        "provider": "github",
        "owner": "Arangdur",
        "repo": "nicos-desktop"
      }
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  },
  "dependencies": {
    "electron-updater": "^6.8.9"
  }
}
